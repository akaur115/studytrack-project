generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Resource {
  id       Int     @id @default(autoincrement())
  name     String
  category String
  source   String
  saved    Boolean @default(false)
}