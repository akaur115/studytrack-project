generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Resource {
  id       Int     @id @default(autoincrement())
  name     String
  category String
  source   String
  saved    Boolean @default(false)
}

model Assignment {
  id        Int     @id @default(autoincrement())
  title     String
  course    String
  priority  String
  dueDate   String
  completed Boolean @default(false)
}

model Assignment {
  id        Int     @id @default(autoincrement())
  title     String
  course    String
  dueDate   String
  completed Boolean @default(false)
}

model ProgressTask {
<<<<<<< HEAD
  id        Int      @id @default(autoincrement())
  task      String
  owner     String
  status    String
  percent   Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  userId Int?
  user   User? @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model User {
  id            Int            @id @default(autoincrement())
  clerkUserId   String         @unique
  email         String?
  name          String?
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  progressTasks ProgressTask[]
}
=======
  id      Int    @id @default(autoincrement())
  title   String
  owner   String
  status  String
  percent Int
}
>>>>>>> edb09f40bc178f5c3cc8576bcbd316a24356be8e
